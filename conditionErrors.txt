if(int x = 3){ // the type of x is int not boolean

};
switch(true){
    case 'a': // the type of the switch case "true" is boolean and not the same as 'a' character 
        int y;
}
while('a'){} // the type of 'a' is character not boolean
for(int i =0;12.5;i = i + 1){ // the condition is of type float "12.5" not boolean
    int z = 1;
}