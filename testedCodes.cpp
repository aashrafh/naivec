#include <bits\stdc++.h>
#include <string>
// 1 main function --> passed
// if condition with logical operators --> passed
int main()
{
    string s = "Hello World";
    const bool f = false;
    if ((x == 10 && y != z) || (f == true && m >= 1000))
    {
        if (m > z || z < v)
            int g = 130 + y * 520;
        else
        {
            char m = 's';
            float s = y - 135.1 * 15.25 / 13.6;
        };
    };
    const int y = 10;
    int x ;
    const int m = 10;
    switch ( x = 10)
    {
    case m:
        break;
    default:
        break;
    }
    while (const int z = 0)
        string s = "string";
    while (true)
    {
        break;
    }

    do 
    {
        do x = 10;
        while (y);
    }
    while(x);
    float z;
    for ( int i = 0; i == 1; z = 10.5)
        for (int j = 0; j < 15; j = 15)
        {
            int x = 10;
            if (x == 10)
            {
                break;
            };
            continue;
//            x = x + -+-+-1;
        }
    return x = 0;
}

void f()
{
    
}

int func()
{
    return 0;
}
int x = func();
/* problems found
    cannot declare a variable inside if/while/switch                     --> solved
    x = x +1; +1 is defined as an integer not as "+ and 1"               --> solved
    in case can put c = 10                                              --> solved
    x = y = z = 10 ;                                                    --> solved
    cannot put i = 10 in for loop                                       --> solved
    return inside if/for inside a function                              
    if if else                                                          --> solved
*/
int i; // 1 main function --> passed